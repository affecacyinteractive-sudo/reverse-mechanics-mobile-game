// types/milestoneFreshness.ts

export type MilestoneFreshness = "FRESH" | "STALE";

// Minimal anchor ref type.
// For v1: anchor refs are IDs from the INDEX namespace (recommended).
export type ProjectionIndexId = string;
